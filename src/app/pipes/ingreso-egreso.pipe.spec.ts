import { IngresoEgresoPipe } from './ingreso-egreso.pipe';

describe('IngresoEgresoPipe', () => {
  it('create an instance', () => {
    const pipe = new IngresoEgresoPipe();
    expect(pipe).toBeTruthy();
  });
});
